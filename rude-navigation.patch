diff --git a/android/sdk/src/main/java/app/organicmaps/sdk/sound/TtsPlayer.java b/android/sdk/src/main/java/app/organicmaps/sdk/sound/TtsPlayer.java
--- a/android/sdk/src/main/java/app/organicmaps/sdk/sound/TtsPlayer.java
+++ b/android/sdk/src/main/java/app/organicmaps/sdk/sound/TtsPlayer.java
@@ -1,5 +1,6 @@
 package app.organicmaps.sdk.sound;
 
+import app.organicmaps.sdk.sound.RudeTts;
 import androidx.annotation.NonNull;
 
@@ -100,7 +101,7 @@
-    final String text = turnNotifications[i];
+    final String text = RudeTts.transform(turnNotifications[i]);

diff --git a/android/sdk/src/main/java/app/organicmaps/sdk/util/Config.java b/android/sdk/src/main/java/app/organicmaps/sdk/util/Config.java
--- a/android/sdk/src/main/java/app/organicmaps/sdk/util/Config.java
+++ b/android/sdk/src/main/java/app/organicmaps/sdk/util/Config.java
@@ -1,6 +1,7 @@
 package app.organicmaps.sdk.util;
 
 import android.content.SharedPreferences;
+import androidx.annotation.Nullable;
 
@@ -1000,6 +1001,9 @@
     String LANGUAGE = "TtsLanguage";
     String VOLUME = "TtsVolume";
     String STREETS = "TtsStreetNames";
+    String RUDE_ENABLED = "TtsRudeEnabled";
+    String USE_YARDS = "TtsUseYards";
+    String DRIVER_NAME = "TtsDriverName";
   }
 
@@ -1010,6 +1014,9 @@
     float VOLUME = VOLUME_MAX;
 
     boolean STREETS = false;
+    boolean RUDE_ENABLED = false;
+    boolean USE_YARDS = false;
+    String DRIVER_NAME = "";
   }
 
@@ -1050,6 +1057,31 @@
   public static void setAnnounceStreets(boolean enabled)
   {
     setBool(Keys.STREETS, enabled);
   }
+
+  public static boolean isRudeEnabled()
+  {
+    return getBool(Keys.RUDE_ENABLED, Defaults.RUDE_ENABLED);
+  }
+
+  public static void setRudeEnabled(boolean enabled)
+  {
+    setBool(Keys.RUDE_ENABLED, enabled);
+  }
+
+  public static boolean useYards()
+  {
+    return getBool(Keys.USE_YARDS, Defaults.USE_YARDS);
+  }
+
+  public static void setUseYards(boolean enabled)
+  {
+    setBool(Keys.USE_YARDS, enabled);
+  }
+
+  public static String getDriverName()
+  {
+    return getString(Keys.DRIVER_NAME, Defaults.DRIVER_NAME);
+  }
+
+  public static void setDriverName(@Nullable String name)
+  {
+    setString(Keys.DRIVER_NAME, name == null ? "" : name);
+  }
 
diff --git a/android/sdk/src/main/java/app/organicmaps/sdk/sound/RudeTts.java b/android/sdk/src/main/java/app/organicmaps/sdk/sound/RudeTts.java
new file mode 100644
--- /dev/null
+++ b/android/sdk/src/main/java/app/organicmaps/sdk/sound/RudeTts.java
@@ -0,0 +1,173 @@
+package app.organicmaps.sdk.sound;
+
+import androidx.annotation.NonNull;
+import app.organicmaps.sdk.util.Config;
+import java.util.Locale;
+import java.util.concurrent.ThreadLocalRandom;
+import java.util.regex.Matcher;
+import java.util.regex.Pattern;
+
+public final class RudeTts
+{
+  private static final Pattern FEET =
+      Pattern.compile("\\b(\\d+)\\s+feet\\b", Pattern.CASE_INSENSITIVE);
+
+  private static int sLastIndex = -1;
+
+  private static final String[] LEFT = {
+      "Turn left, you fucking muppet.",
+      "Left turn. Try not to miss the bastard.",
+      "Turn left, for fuck's sake.",
+      "Take the left, you absolute bellend."
+  };
+
+  private static final String[] RIGHT = {
+      "Turn right, you fucking muppet.",
+      "Right turn. Pay attention this time.",
+      "Turn right, for fuck's sake.",
+      "Take the right, you absolute bellend."
+  };
+
+  private static final String[] RECALC = {
+      "Right, you've fucked it. Recalculating.",
+      "For fuck's sake. Recalculating.",
+      "You've ignored me, so I'm fixing your mess.",
+      "Brilliant. Now I have to find another way."
+  };
+
+  private static final String[] WRONG = {
+      "You're on the wrong fucking route.",
+      "Wrong route, you absolute muppet.",
+      "Nope. That's completely fucking wrong.",
+      "Congratulations, you've gone the wrong way."
+  };
+
+  private static final String[] DEST = {
+      "You're nearly there. Miraculously.",
+      "Destination approaching. Don't fuck it up now.",
+      "Almost there, you magnificent fucking disaster.",
+      "You actually made it."
+  };
+
+  private static final String[] KEEP_LEFT = {
+      "Keep left, you muppet.",
+      "Keep left. It's not fucking difficult."
+  };
+
+  private static final String[] KEEP_RIGHT = {
+      "Keep right, you muppet.",
+      "Keep right. Pay fucking attention."
+  };
+
+  private static final String[] UTURN = {
+      "Make a U-turn, you absolute bellend.",
+      "Turn around. Yes, you've fucked it."
+  };
+
+  private static final String[] ROUNDABOUT = {
+      "Roundabout ahead. Try counting this time.",
+      "Approaching a roundabout. Don't fuck it up."
+  };
+
+  private static final String[] SPEEDING = {
+      "Slow the fuck down, %s.",
+      "You're speeding, %s. Ease off the fucking accelerator.",
+      "Oi %s, the speed limit isn't a suggestion."
+  };
+
+  private static final String[] BRAKING = {
+      "Easy on the brakes, %s. That was fucking harsh.",
+      "Bloody hell, %s. That's some serious braking.",
+      "Try braking like a normal person, %s."
+  };
+
+  private static final String[] CORNER = {
+      "That's a bit fucking quick for this bend, %s.",
+      "Slow down for the bend, %s.",
+      "Easy into that corner, %s."
+  };
+
+  private RudeTts() {}
+
+  @NonNull
+  public static String transform(@NonNull String text)
+  {
+    if (!Config.TTS.isRudeEnabled() || !isEnglish())
+      return text;
+
+    String out = convertFeetToYards(text);
+    String lower = out.toLowerCase(Locale.US);
+    String extra = null;
+
+    if (lower.contains("recalculat"))
+      extra = pick(RECALC);
+    else if (lower.contains("wrong route") || lower.contains("route has changed"))
+      extra = pick(WRONG);
+    else if (lower.contains("destination") || lower.contains("you have arrived")
+             || lower.contains("arrived"))
+      extra = pick(DEST);
+    else if (lower.contains("u-turn") || lower.contains("u turn"))
+      extra = pick(UTURN);
+    else if (lower.contains("roundabout"))
+      extra = pick(ROUNDABOUT);
+    else if (lower.contains("keep left"))
+      extra = pick(KEEP_LEFT);
+    else if (lower.contains("keep right"))
+      extra = pick(KEEP_RIGHT);
+    else if (lower.contains("turn left") || lower.contains("left"))
+      extra = pick(LEFT);
+    else if (lower.contains("turn right") || lower.contains("right"))
+      extra = pick(RIGHT);
+
+    return extra == null ? out : out + " " + extra;
+  }
+
+  public static String speedingComment()
+  {
+    return String.format(Locale.US, pick(SPEEDING), driverName());
+  }
+
+  public static String harshBrakingComment()
+  {
+    return String.format(Locale.US, pick(BRAKING), driverName());
+  }
+
+  public static String fastCornerComment()
+  {
+    return String.format(Locale.US, pick(CORNER), driverName());
+  }
+
+  private static String driverName()
+  {
+    String name = Config.TTS.getDriverName().trim();
+    return name.isEmpty() ? "driver" : name;
+  }
+
+  private static boolean isEnglish()
+  {
+    String lang = Config.TTS.getLanguage();
+    return lang == null || lang.isEmpty()
+           || lang.toLowerCase(Locale.US).startsWith("en");
+  }
+
+  @NonNull
+  private static String convertFeetToYards(@NonNull String text)
+  {
+    if (!Config.TTS.useYards())
+      return text;
+
+    Matcher m = FEET.matcher(text);
+    StringBuffer b = new StringBuffer();
+
+    while (m.find())
+    {
+      int feet = Integer.parseInt(m.group(1));
+      int yards = Math.max(1, Math.round(feet / 3.0f));
+      m.appendReplacement(b, yards + " yards");
+    }
+
+    m.appendTail(b);
+    return b.toString();
+  }
+
+  private static String pick(String[] values)
+  {
+    if (values.length == 1)
+      return values[0];
+
+    int index;
+    do
+    {
+      index = ThreadLocalRandom.current().nextInt(values.length);
+    }
+    while (index == sLastIndex);
+
+    sLastIndex = index;
+    return values[index];
+  }
+}
