diff --git a/android/app/src/main/java/app/organicmaps/settings/VoiceInstructionsSettingsFragment.java b/android/app/src/main/java/app/organicmaps/settings/VoiceInstructionsSettingsFragment.java
--- a/android/app/src/main/java/app/organicmaps/settings/VoiceInstructionsSettingsFragment.java
+++ b/android/app/src/main/java/app/organicmaps/settings/VoiceInstructionsSettingsFragment.java
@@ -1,6 +1,7 @@
 package app.organicmaps.settings;
 
 import android.os.Bundle;
+import android.text.InputType;
 import android.view.View;
 import androidx.annotation.NonNull;
 import androidx.annotation.Nullable;
@@ -10,6 +11,7 @@
 import androidx.preference.Preference;
 import androidx.preference.PreferenceFragmentCompat;
 import androidx.preference.TwoStatePreference;
+import androidx.preference.EditTextPreference;
 
 import app.organicmaps.sdk.util.Config;
 
@@ -20,6 +22,8 @@
   private TwoStatePreference mTtsRude;
   private TwoStatePreference mTtsYards;
+  private EditTextPreference mTtsDriverName;
 
   @Override
   public void onCreatePreferences(@Nullable Bundle savedInstanceState, @Nullable String rootKey)
@@ -30,6 +34,8 @@
     mTtsRude = findPreference("pref_tts_rude");
     mTtsYards = findPreference("pref_tts_yards");
+    mTtsDriverName = findPreference("pref_tts_driver_name");
 
     if (mTtsRude != null)
     {
@@ -41,6 +47,25 @@
       mTtsRude.setOnPreferenceChangeListener((preference, newValue) ->
       {
         Config.TTS.setRudeEnabled((Boolean) newValue);
         return true;
       });
     }
 
+    if (mTtsDriverName != null)
+    {
+      mTtsDriverName.setText(Config.TTS.getDriverName());
+      mTtsDriverName.setOnPreferenceChangeListener((preference, newValue) ->
+      {
+        Config.TTS.setDriverName((String) newValue);
+        return true;
+      });
+    }
+
     if (mTtsYards != null)
     {
       mTtsYards.setChecked(Config.TTS.useYards());
@@ -54,3 +79,4 @@
   }
 }
diff --git a/android/sdk/src/main/java/app/organicmaps/sdk/util/Config.java b/android/sdk/src/main/java/app/organicmaps/sdk/util/Config.java
--- a/android/sdk/src/main/java/app/organicmaps/sdk/util/Config.java
+++ b/android/sdk/src/main/java/app/organicmaps/sdk/util/Config.java
@@ -1,3 +1,4 @@
 package app.organicmaps.sdk.util;
 
 public final class Config
 {
@@ -10,6 +11,7 @@
   public static final class TTS
   {
     private static final String RUDE = "TtsRude";
     private static final String YARDS = "TtsYards";
+    private static final String DRIVER_NAME = "TtsDriverName";
 
     private static final boolean DEFAULT_RUDE = false;
     private static final boolean DEFAULT_YARDS = true;
+    private static final String DEFAULT_DRIVER_NAME = "";
 
     public static boolean isRudeEnabled()
     {
       return getBool(Keys.RUDE, Defaults.RUDE);
     }
 
     public static void setRudeEnabled(boolean enabled)
     {
       setBool(Keys.RUDE, enabled);
     }
 
     public static boolean useYards()
     {
       return getBool(Keys.YARDS, Defaults.YARDS);
     }
 
     public static void setUseYards(boolean enabled)
     {
       setBool(Keys.YARDS, enabled);
     }
+
+    public static String getDriverName()
+    {
+      return getString(Keys.DRIVER_NAME, Defaults.DRIVER_NAME);
+    }
+
+    public static void setDriverName(String name)
+    {
+      setString(Keys.DRIVER_NAME, name == null ? "" : name.trim());
+    }
   }
 }
diff --git a/android/sdk/src/main/res/values/strings.xml b/android/sdk/src/main/res/values/strings.xml
--- a/android/sdk/src/main/res/values/strings.xml
+++ b/android/sdk/src/main/res/values/strings.xml
@@ -1,3 +1,7 @@
 <resources>
+  <string name="pref_tts_driver_name">tts_driver_name</string>
+  <string name="pref_tts_driver_name_title">Driver name</string>
+  <string name="pref_tts_driver_name_summary">Name used in rude driving comments.</string>
+
   <string name="pref_tts_rude">tts_rude</string>
   <string name="pref_tts_rude_title">Rude navigation voice</string>
   <string name="pref_tts_rude_summary">Add random sweary commentary to English voice directions.</string>
diff --git a/android/sdk/src/main/res/xml/prefs_voice_instructions.xml b/android/sdk/src/main/res/xml/prefs_voice_instructions.xml
--- a/android/sdk/src/main/res/xml/prefs_voice_instructions.xml
+++ b/android/sdk/src/main/res/xml/prefs_voice_instructions.xml
@@ -1,6 +1,12 @@
 <PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
     xmlns:app="http://schemas.android.com/apk/res-auto">
 
+    <EditTextPreference
+        android:key="pref_tts_driver_name"
+        android:title="@string/pref_tts_driver_name_title"
+        android:summary="@string/pref_tts_driver_name_summary"
+        android:dialogTitle="@string/pref_tts_driver_name_title"
+        android:inputType="textCapWords"
+        app:useSimpleSummaryProvider="true" />
+
     <androidx.preference.SwitchPreferenceCompat
         android:key="pref_tts_rude"
         android:title="@string/pref_tts_rude_title"
@@ -10,4 +16,10 @@
         android:title="@string/pref_tts_yards_title"
         android:summary="@string/pref_tts_yards_summary" />
 
 </PreferenceScreen>
diff --git a/android/sdk/src/main/java/app/organicmaps/sdk/sound/TtsPlayer.java b/android/sdk/src/main/java/app/organicmaps/sdk/sound/TtsPlayer.java
--- a/android/sdk/src/main/java/app/organicmaps/sdk/sound/TtsPlayer.java
+++ b/android/sdk/src/main/java/app/organicmaps/sdk/sound/TtsPlayer.java
@@ -1,5 +1,6 @@
 package app.organicmaps.sdk.sound;
 
+import app.organicmaps.sdk.sound.RudeTts;
 
 ...
- final String text = turnNotifications[i];
+ final String text = RudeTts.transform(turnNotifications[i]);
